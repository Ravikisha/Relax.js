# Benchmarks (scaffold)

This folder contains the benchmark harness and benchmark cases described in `tasks.md`.

Planned:
- [x] `harness.ts`
- [x] `cases/`
- [x] `adapters/` *(scaffold folder)*

See `docs/benchmarks.md` for methodology and how to run.
