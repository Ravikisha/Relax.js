import { type VNode } from './h';
/**
 * Traverses the vdom tree, finds the first slot node and replaces it with the
 * passed in children.
 */
export declare function fillSlots(vdom: any, children: VNode[]): void;
//# sourceMappingURL=slots.d.ts.map